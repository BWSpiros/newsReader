<ul>
  <% feeds.each(function(feed){ %>
    <li><a href='#/feeds/<%= feed.get('id') %>' class='feedLink' data-id='<%= feed.get('id') %>'>
      <%= feed.get('title') %>
    </a></li>
  <% }); %>
</ul>
<button id="refresh">Refeeeeeed</button>